# OpenAI 兼容 API（支持 OpenAI / vLLM / Ollama / DeepSeek 等）
OPENAI_API_KEY=sk-your-api-key
OPENAI_BASE_URL=https://api.openai.com/v1

# 可选：为不同角色指定不同模型（留空则使用 config.yaml 中的默认值）
# EXPERT_MODEL=gpt-4o-mini
# JUDGE_MODEL=gpt-4o
